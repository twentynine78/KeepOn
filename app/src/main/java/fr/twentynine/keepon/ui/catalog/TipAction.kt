package fr.twentynine.keepon.ui.catalog

/** The action a tip's primary button triggers, mapped by the screen to the matching UI event. */
enum class TipAction {
    RequestPostNotification,
    RequestAddTileService,
    RequestAppRate,
}
