// Domain models and catalogs are pure Kotlin (no Compose dependency) yet are
// read by the Compose UI. Declaring them stable here lets the Compose compiler
// skip recomposition without leaking @Stable/@Immutable into the domain layer.
fr.twentynine.keepon.domain.model.*
fr.twentynine.keepon.domain.catalog.*
